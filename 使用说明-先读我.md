# 灰烬之下 — 能不能在 Unity 里跑起来？

## 结论

**原 zip 里的东西跑不起来，因为它根本不是一个 Unity 工程。**

`D:\我的项目\灰烬之下` 里只有一个 `README.md等11个文件.zip`，解压后是 **9 个 C# 脚本 + 2 份 Markdown**。
没有 `Assets/`、没有 `ProjectSettings/`、没有 `Packages/`、没有 `.unity` 场景、没有 `.meta`。
Unity 无法打开一个只有代码文件的文件夹 —— 这些脚本只是"零件"，还缺车架、轮子、方向盘。

本目录（`Ashfall/`）就是补上的那台车：**一个可以直接用 Unity 6000.3.21f1 打开的最小工程**。

---

## 三步跑起来

1. **Unity Hub → Open → Add project from disk**，选这个 `Ashfall` 文件夹。
   首次打开会导入资源、生成 Library，需要几分钟（命令行模式在这台机器上会卡死，图形界面正常）。

2. 打开后，菜单栏点 **灰烬之下 → 一键搭建可运行场景**。
   它会自动建好：Grid + Tilemap（DigGrid）、Player（掘进服）、GameManager、地表基地、相机跟随，
   并生成方块/矿物数据库、保存成 `Assets/Scenes/Game.unity`、加进 Build Settings。

3. 点 **Play**。
   - `WASD / 方向键` 移动
   - **朝着方块按住方向键** = 钻探
   - 回到地表坑口自动卖矿 / 加油 / 维修
   - `U` 打开升级商店，`1`~`6` 购买对应部件

> 如果控制台提示 "Active Input Handling 已改为 Both"，**重启一次 Unity 再 Play**。
> 这是为了让 `Input.GetKey` 这类旧版输入 API 生效（见下文「我改了什么」第 4 条）。

---

## 工程构成

```
Ashfall/
├── ProjectSettings/ProjectVersion.txt   # 锁定 6000.3.21f1
├── Packages/manifest.json               # 只依赖 Unity 内置包，不联网
└── Assets/Scripts/
    ├── 原 9 个脚本（原样保留）
    │   TileDefinition / TileDatabase / DigGrid / DrillVehicle
    │   UpgradeSystem / SurfaceBase / GameManager / GameHUD
    ├── CameraFollow.cs      【新增】相机跟随 + 深度越深视野越远
    ├── PlayerVisual.cs      【新增】运行时程序生成圆点贴图，零美术依赖
    └── Editor/
        ├── AshfallSetup.cs            【原】一键生成方块与数据库
        └── AshfallSceneBuilder.cs      【新增】一键搭建完整场景
```

**渲染管线用 Built-in，不用 URP。** 这套游戏是纯色块 Tilemap，URP 带来的只有额外导入时间和后处理干扰。

---

## 我改了什么（相对原 zip）

只有 3 处，都在副本上，**你的原文件一个字节都没动**：

### 1. 加了网格碰撞 —— 没有它，游戏不成立

`DrillVehicle.FixedUpdate` 原本直接 `rb.linearVelocity = input * speed`。
场景里若没有 `TilemapCollider2D`，**玩家会直接穿过所有方块**，"钻探"完全失去意义。

但给 48×520 = 24960 格挂 `TilemapCollider2D` 是灾难：每挖一格触发 chunk 碰撞体重建，深挖时持续卡顿。

所以改成**网格级碰撞**（`DigGrid.IsSolidAtWorld` + `DrillVehicle.BlockedByTerrain`）：
移动前用玩家包围盒四角采样目标格，实心就清掉该轴速度。
玩家被"顶住" → `HandleDigging` 探测到前方实心格 → 开始钻 → 钻穿后格子变空 → 玩家进去。
**O(1) 开销，零碰撞体重建。**

### 2. 加了相机跟随

原代码没有任何相机逻辑，玩家一移动就跑出画面。

### 3. 加了玩家外观

原代码只有 `Rigidbody2D`，`SpriteRenderer` 没贴图 = 屏幕上什么都看不见。
`PlayerVisual` 用代码生成一个带抗锯齿的蓝黄圆点，仍然是零美术依赖。

### 4. 输入读取不再依赖轴向配置

`Input.GetAxisRaw("Horizontal")` 依赖 `ProjectSettings/InputManager.asset` 里预置的轴向。
新建工程不保证有，改成直接读 `KeyCode.W/A/S/D/方向键`，自包含。

配套：`AshfallSceneBuilder` 会自动把 **Active Input Handling** 设为 **Both**，
因为 `Input.GetKey` / `GetKeyDown` 属于旧版输入 API，在"仅新输入系统"模式下运行时会抛异常。

---

## 还没修的问题（这版只是"能跑"，不是"好玩"）

按之前评估的结论，这些是 P0，一个都没动：

| 问题 | 后果 |
|---|---|
| **BALANCE v2 未落地** | 跑的是 v1 数值：价格 `base×lv^1.6`（后期一夜暴富）、油箱 `100+40/级`、深度 520、矿物价值全错 |
| **散热安全深度耦合** | 换算劈在 `DrillVehicle` 和 `UpgradeSystem` 两处，v2 的 `250+70/级` 根本凑不出来 |
| **死亡零惩罚** | 只清空货舱不扣现金 → 回程主动烧完燃料 = 免费满状态回家，核心张力失效 |
| **货舱无丢弃/替换** | 下潜路上被廉价矿塞满，深层白跑；满舱提示会常驻刷屏 |
| **落石无预警** | 数据层瞬移换格，玩家莫名其妙掉血 |
| **`isDigging` 判定顺序反了** | 对着挖不动的硬岩按住方向键，进度条照走 + 按钻探油耗扣燃料 |

---

## 附：为什么不用 Unity 的 2D 模板

试过官方 2D 模板（`com.unity.template.2d-cross-platform-2d-6.1.2.tgz`，Unity 安装目录自带，3.9 MB / 64 个文件，零美术资源）。
它引入了 **URP 渲染管线**和**新输入系统**，而这 9 个脚本是 Built-in 时代的写法（`Input.GetAxisRaw` + IMGUI），本来就没适配它们 —— 用了反而制造不兼容。

更关键的是：**这台机器上 Unity 的命令行模式（batchmode）做首次资源刷新会挂死**，
两次尝试都停在同一行 `Application.AssetDatabase Initial Refresh Start`，第二次已去掉 URP 照样卡。
所以本工程手写而成，避开联网和大包，**请务必用 Unity Hub 图形界面打开**。
