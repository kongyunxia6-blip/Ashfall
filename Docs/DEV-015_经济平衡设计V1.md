# DEV-015 · 资源/深度经济平衡 V1 — 设计 + 经济表

仓库：Ashfall（`kongyunxia6-blip/Ashfall`），分支 `wb/dev-015-economy-balance-v1`，基座 `main@b52b96501a941fa2bb900f16871e1deef3cc4e64`。
Issue #32 校准目标（经确认）：**现代 DEV 栈**（OreVeinGenerator bands + TileDefinition 资产 + EquipmentProgression/EquipmentCatalog + InventoryGrid + RunRiskState + DiscoveryNode + SellTerminal）。不改旧 Game.unity 的 UpgradeSystem/TileDatabase.layers（第二系统，Issue 禁止改）。

> 本 DEV 不新增大型系统、不建第二套 Currency / Cargo / Fuel / Region / Ore generator / 运行时 EconomyManager。只校准现有参数 + 新增「纯数据配置 + Editor-only 确定性模拟/报表」。
> 永久规则：1 Block=1×1、单格采矿、禁 AoE / chain / ExplosiveRock、Scanner 只读。

---

## 1. 核心决策问题 → 设计支点

Issue 8 问，落在现代 DEV 栈的答案：

1. **Cargo 已经很值钱还要深入吗** → 靠「单趟载重上限（重量先到顶）+ 越深越重越贵但单趟可带件数减少 + 返航燃料余量」制造机会成本。Cargo 价值随深度上升，但**收益不是线性**：深层高单价矿单件重、堆叠少 → 重量预算被高单价单件吃紧 → 「这趟满载回去，还是再下去冒更大返航风险」真实成立。
2. **单价高但太重值得占格吗** → 价值密度 valuePerWeight 分层：铁/锡/铜低密度，银/金中，铂金/祖母绿/钻石高密度但单件更重。`HandleTileDug` 已按**价值密度**丢最差换入 → 玩家会为高密度资源放弃低密度堆（铁矿 vs 钻石）。
3. **Fuel 够安全返航吗** → 载重爬升油耗惩罚 `×upwardFuelMultiplier×(1+LoadRatio×weightFuelPenalty)` + 满载返回；深 Run 满载返航的余量显著恶化。EfficientMotor(−15%) 与 FuelTank(上限) 是两条不同维度的解药。
4. **为 Discovery Node 绕路值得吗** → 节点奖励按 region 价值最高的矿种；但绕路消耗燃料 + 载重被占用 → 至少一种配置下「放弃节点直接返航」更优。
5. **深层矿是更值钱还是单位重量更值钱/更稀有/更危险** → 用多维：深层 = **更高价值密度 + 更稀有 + 更重 + 更高硬度门槛 + 风险(坍塌/热/遗迹)** 共同变化（≥3 维），非单乘价。
6. **升级价节奏** → 调 EquipmentCatalog：Lv1 首升约 1 个成功浅层 Run；中段 1~3 Run；高阶需 Mid/Deep 高质量货（浅层刷不出）。
7. **主动丢弃低值资源** → `HandleTileDug` 价值密度替换即机制；验收用 Play 场景复现「铁矿堆被钻石顶掉」。
8. **见好就收节点** → 存在「本趟能安全返航」的满载点；再深入则返航余量转负 → 最优策略是折返，不是挖到硬上限。

---

## 2. 资源经济表 V1（现代 DEV 栈可及资源）

valuePerWeight = value/weight；单格价值上限 = value（gridWidth=1 单格 1 件起，堆叠由 stackLimit 与载重共同约束）。意图角色：

| 资源 | 主带 | hardness | baseValue | weight | stackLimit | valuePerWeight | 意图角色 |
|---|---|---|---|---|---|---|---|
| Iron 铁矿 | Shallow | 1 | (校准) | 轻 | 16 | 低 | 基础填充矿，几乎只练手感/垫舱，不应刷钱 |
| Tin 锡矿 | Shallow | 1 | (校准) | 轻 | 16 | 低 | 基础矿，量多价低 |
| Copper 铜矿 | Shallow/Mid | 1 | (校准) | 中轻 | 16 | 低-中 | Shallow 主收益；Mid 权重下降 |
| Silver 银矿 | Mid | 2 | (校准) | 中 | 16 | 中 | Mid 主收益门槛矿(硬度2) |
| Gold 金矿 | Mid/Deep | 2 | (校准) | 中重 | 16 | 中高 | Deep 可见；高密度起步 |
| Emerald 绿宝石 | Deep | 3 | (校准) | 重 | 16 | 高 | Deep 高密度单件 |
| Platinum 铂金 | Deep | 3 | (校准) | 重 | 16 | 高 | Deep 高密度 |
| Ruby 红宝石 | Deep(末) | 4 | (校准) | 很重 | 16 | 很高 | Deep 深处稀有、高密度、高门槛 |
| Diamond 钻石 | Deep | 4 | (校准) | 最重 | 16 | 很高 | 高价值密度极值，重的代价明显 |
| AncientDataFragment | C 节点(Deep) | 1 | (校准) | 极轻 | 16 | 高 | 文明研究价值：轻而贵、仅节点可得 → 诱惑绕路 |
| AncientAlloy | C 节点/Ruin | 2 | (校准) | 轻 | 16 | 中高 | 文明材料 |

设计约束（防支配资源）：**不允许同一带内某资源同时在价值、重量、堆叠、稀有度上全方位最优**。例如 Deep 钻石最贵也最重；DataFragment 很轻但仅节点 1 格且需绕路；铁/锡最轻但单价垫底。

---

## 3. 深度价值曲线 V1（≥3 维共同变化，非单乘价）

| Region | 深度 | 单位重量价值 | 单格价值 | 稀有度/权重 | vein 大小 | 环境/门槛 |
|---|---|---|---|---|---|---|
| Surface | 0..2 | — | — | 无矿 | — | 起点/Hub |
| Shallow | 3..21 | 低 | 低~中 | 铁/锡/铜 密集 | 小(2~4) | 无门槛 |
| Mid | 22..43 | 中 | 中~高 | 铜/银/金 出现 | 中(3~5) | 硬度 2 银/金 |
| Deep | 44..62 | **高** | 高~很高 | 金/铂/祖母绿/钻石/文明 | 大(4~6)但单件重 | 硬度 3~4 + 风险节点 |

四维共同变化：**(1) valuePerWeight↑ (2) valuePerSlot↑ (3) 稀有度(高价值矿权值低→少见) (4) vein 中更有重单件 + 硬度门槛 + Discovery/Ruin 风险**。Shallow 无法成为无限刷钱最优解——因为 Lv1 装备升级到中段后 Shallow 单趟价值增长被载重/返航余量封顶，Mid/Deep 的高密度单件是唯一拉开单趟收益的路径。

---

## 4. 模拟模型（Editor-only 确定性，不参与 runtime）

输入：region 典型矿脉组成（band 权重抽样）、Cargo capacity/weight cap、Fuel cap、equipment level、路径长度/深度。
输出：expected Cargo value / weight / value-per-weight / Fuel used & remaining / safe-return margin / expected runs to next upgrade。
公式取真实现：
- Fuel：idle 0.5/s；移动 1.1×moveMult；上行 ×2.2×(1+LoadRatio×0.35)；挖掘 +2.0/s。
- 满载惩罚：LoadRatio 影响上行油耗。
- 载重上限：Lv0 24 / Lv1 30 / Lv2 37 / Lv3 45（+CargoRack +6）。
- 格数：6×rows(2..5)。
- 升级成本：见 §6。

---

## 5. Cargo 取舍场景（可复现）

「装铁矿堆（12/块）途中挖到钻石(11000/块)：载重满 → HandleTileDug 按价值密度丢掉铁矿，把钻石装进」→ Play 断言铁 count 下降 / CargoValue 跳升。此即 Issue「丢低值给高值腾位」验收。

---

## 6. 升级节奏目标 & EquipmentCatalog 成本校准

节奏：Lv1 首升≈1 浅层 Run；中段 1~3 Run；Lv3 需 Mid/Deep 高质量货。按目标单趟收益设定成本上限（校准后由模拟核对「不出现一次普通 Run 直接买满多级核心升级 / 不长期无进展」）。

---

## 7. 验证结果 V1（Unity MCP 实测，2026-09-07）

### 7.1 确定性经济回归（`EconomyBalanceV1Regression`，只读真实资产，不改系统）

菜单「灰烬之下 → DEV-015 回归：资源/深度经济平衡 V1」→ **PASS 34 / FAIL 0**，结果 `d15_regression_result.txt`：
- S1 资源表 v/w 严格分层：Iron 12 < Tin 18 < Copper 33 < Silver 87 < Gold 137 < Emerald 218 < Platinum 346 < Ruby 433 < Diamond 743；钻石最贵也最重(3.5) → 无「同带全方位最优」支配资源；DataFragment v/w 450 > Gold 且极轻 → 节点诱惑但需绕路。
- S2 深度价值阶梯：Shallow 主带 max v/w 33 < Mid 137 < Deep 743；深矿 hardness 2..4 递升 + 更重 → 多维度而非单乘价。
- S3 主动丢弃低值：真实 `InventoryGrid` Lv0 载重满(24 铁/$288)，钻石(v/w 743)顶掉 3~4 件铁 → CargoValue 288→2840(+2552)、铁数下降、载重 23.5≤24。
- S4 三套装备 Run：Shallow Lv0 满载返航余量 +61 / Mid −11 / Deep −69（真实 1.1×2.2×(1+0.35) 上行公式）→ 越深返航越险，见好就收节点存在。
- S5 升级节奏：Lv1≈0.3~0.6 浅层 Run（首升 1 趟可达）；Lv1+Lv2 需 >1 趟（不会一次返航买满两级核心）；Lv3 浅层单刷需 3.8~7.5 趟 → 高阶被 Deep/Mid 高质量货门槛卡住；成本单调递增。
- S6 Discovery Node：绕路 12 格额外耗油 ~39；Mid/Deep Lv0 满载 + 节点绕路 → 返航不安全 → 至少一档「放弃节点直接返航」更优（见好就收 + 风险权衡）。

### 7.2 真实 Unity Play 闭环（`EconomyBalanceV1Probe` @ `EconomyBalanceV1Test` 场景）

真实 Run（RunRiskState 生命周期，非手动）→ **PASS 26 / FAIL 0**，结果 `d15_play_result.txt`：
- E1/E2 地表基线 → 真实下潜 21 格离开地表 → RunRiskState 状态机自启 Run#1（RunNumber 0→1，非手动 BeginRun）。
- E3 真实挖 24 块 Iron 填满 Cargo（w 24/24、$288，HandleTileDug 入包，真实 DrainFuel 计耗）。
- E4 **Cargo 选择**：挖到紧邻高值 Emerald → HandleTileDug 顶掉 3 件最低密度 Iron（24→21），CargoValue 288→732(+444)、载重 23.2≤24 → 「丢铁矿换高值」真实成立。
- E5 返航决策：真实剩油 111 > 上行需 71.3（余量 +39.8）；净余油仅够再下潜 ≤12.3 格 → 有限折返点，不是无限可继续。
- E6/E7 真实返航地表 → 驶入 SellTerminal（PlayerInRange=true）→ TrySell 增收 → RunActive=false（本 Run 收官）。

### 7.3 DEV-007~014 全量回归（无回归）

| 回归 | 结果 |
|---|---|
| DEV-009（覆盖 007矿脉 / 008空间，22 seed×12 断言） | FAIL 0 / 264 |
| DEV-011（Cargo Loss + Recovery Fee 矩阵） | PASS 15 / FAIL 0 |
| DEV-012（覆盖 010装备，地层/能力/矿物矩阵） | PASS 36 / FAIL 0 |
| DEV-013（遗迹数据/生成/封印元数据） | PASS 30 / FAIL 0 |
| DEV-014（发现节点组合/落点/原子/确定性） | PASS 21 / FAIL 0 |
| Console | 0 Error / 0 Exception / 0 Warning |

### 7.4 校准后确定性模拟（`EconomyBalanceV1Sim`，Shallow/Mid/Deep × Lv0/Lv1/Lv2）

结果 `d15_economy_sim.txt`（逐块价值密度替换 + 真实燃料模型）：
- 深度单趟收益分层：Shallow Lv0 ≈$424（返航余量 +43.6 安全）/ Mid Lv0 ≈$800（余量 −49.7 需燃料仓）/ Deep 需 Lv2 燃料。
- 升级节奏：Shallow 首升≈1 趟；Deep Lv2 cargo ≈$29,760（高价值但需返航能力 + 载重代价）。

