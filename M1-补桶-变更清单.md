# M1 补桶 — 变更清单

> 目标：把 BALANCE v2 数值落地，并补掉三个会拆掉核心张力的漏洞。
> **改完后必须重跑一次菜单才会生效**（见文末「如何生效」）。

---

## ⚠️ 如何生效（重要）

场景 `Assets/Scenes/Game.unity` 里**序列化保存着旧值**（`depth: 520`、`baseSafeDepth: 180`、`fuelPerTankLevel: 40`），
Unity 的序列化值会覆盖 C# 字段默认值，所以**只改代码不改场景是不生效的**。

菜单 → **灰烬之下 → 一键搭建可运行场景**，重跑一次即可。它会：

1. 调用 `AshfallSetup.CreateDefaults()` → 用 v2 数值**就地更新**已存在的方块资产
2. `NewScene` 重建场景 → 所有 C# 默认值（深度 640、安全深度 250 等）重新写入

---

## 1. v2 数值落地（BALANCE 第九节 14 项）

| 项 | 原值 | v2 | 文件 |
|---|---|---|---|
| 升级价格公式 | `base × level^1.6` | `base × 8^(level-1)` | UpgradeSystem.GetCost |
| 钻头 base | 200 | **1,000** | UpgradeSystem.GetBaseCost |
| 货舱 base | 400 | 700 | 同上 |
| 油箱 base | 250 | 600 | 同上 |
| 散热 base | 280 | 500 | 同上 |
| 船体 base | 300 | 450 | 同上 |
| 引擎 base | 150 | 400 | 同上 |
| 油箱容量 | `100 + 40/级` | **`120 + 140/级`** | UpgradeSystem.MaxFuel |
| 安全深度 | `180 + 60/级` | `250 + 70/级` | UpgradeSystem.SafeDepth |
| 地图深度 | 520 | **640** | DigGrid.depth + 场景搭建器 |
| 矿物价值 | 5/15/45/120/300/500/900/1500/5000 | **12/40/130/400/900/2200/5000/11000/40000** | AshfallSetup |
| 分层起点 | 0/20/60/120/200/280/360/450/500（9 层） | **0/70/110/180/250/330/420/500/580/620（10 层）** | AshfallSetup |
| 分层权重 | 旧表 | 按 BALANCE 第三节 | AshfallSetup |
| 等级上限 | 无 | **钻头 Lv5，其余 Lv6** | UpgradeSystem.GetMaxLevel |

**油箱改动最关键**：原值 `100+40/级` 在阶段 2 就会出现「下去了上不来」的硬伤。
**等级上限**：原原型钻头能买到 Lv6，花 3276 万却毫无作用（Lv5 已能挖穿最硬的矿）。

---

## 2. 散热解耦 🔧

原来换算被劈成两半，v2 的 `+70/级` 根本凑不出来：

```csharp
// DrillVehicle：写死 60/级
float safe = baseSafeDepth + (upgrades.Cooling - 1f) * 60f;
// UpgradeSystem：系数
Cooling => 1f + (radiatorLevel - 1) * coolingPerRadiatorLevel;  // 要凑 70 得填 1.1667
```

现在**单点定义**，UpgradeSystem 直接暴露安全深度：

```csharp
public float SafeDepth => 250f + (radiatorLevel - 1) * safeDepthPerRadiatorLevel;  // 70
```

DrillVehicle 只负责读取，拿不到 UpgradeSystem 时才回退到 `baseSafeDepth`（已同步改为 250）。

---

## 3. 死亡惩罚（堵死"免费传送"漏洞）💀

**原漏洞**：`OnPlayerDied` → 延迟 2.5s → `RespawnAtSurface` → `FullRestore()` 满油满血，**现金一分不扣**。
于是最优解是：回程时货舱空了就**主动把燃料烧完 = 免费传送回地表 + 满状态**，还省了爬上去的油钱和时间。
而「回程燃料够不够」恰恰是这类游戏全部紧张感的来源。

**现在三层轻惩罚**：

1. 载荷全部损失
2. **现金扣打捞费** = `min(现金 × 15%, 100 + 死亡深度 × 2)`
3. **重生只恢复** 能源 60% / 服体 50%（原来是 100%）

**验算**（深度 200 死，载荷 ¥3,000、现金 ¥5,000）：

| | 正常回家 | 主动自杀 |
|---|---|---|
| 打捞费 | ¥0 | **−¥500** |
| 补油差额 | −¥20 | −¥42 |
| **净额** | **−¥20** | **−¥590 + 等 3 分钟** |

漏洞消失。深处惩罚故意温和（取 `min` 后打捞费封顶），因为惩罚太重玩家就不去回捞了 —— 残骸回捞（M4）会白做。

---

## 4. 载荷自动替换 🎒

**原问题**：下潜路上要挖穿几百格，泥土和铁矿会塞满 20 格货舱；等到了银矿区货舱已满，
提示「矿物被丢弃」，一趟下来赚的全是一路捡的破烂。这是 Motherload 原作的经典痛点。
附带问题：满舱后每挖一格弹一次提示（钻一格 0.3s、提示持续 1.2s）—— **提示常驻刷屏**。

**现在**：
- 货舱改为保存**每块矿的价值明细**（`List<int> cargo`），不再只存总数
- 满舱时：新矿比舱内**最便宜的那块**值钱 → 丢掉最便宜的换进来
- 提示加**节流**（同一条 1.5s 内不重复弹）

---

## 5. isDigging 判定顺序反了（真 Bug）🐛

```csharp
// 原：先标记"正在钻"，再判挖不动
digTargetTime = def.drillTime;
isDigging = true;
...
if (def.hardness > drillLevel) { ShowMessage("钻头不足"); return; }
```

后果：对着挖不动的硬岩按住方向键，**进度条照走（假动作）且按钻探油耗 2.0/s 照扣**，玩家白烧油。
现在硬度检查提到设置 `isDigging` 之前，并先 `ResetDig()`。

---

## 6. 附带修正

- **商店满级显示**：`GetCost` 满级返回 0，直接显示会变成误导性的 `$0`。现在显示「已满级」并置灰。
- **安全深度兜底**：`baseSafeDepth` 180 → 250，与 v2 的散热 Lv1 基准对齐。

---

## 还没做（后续里程碑）

| 里程碑 | 内容 |
|---|---|
| M2 | 落石预警（现在是数据层瞬移，玩家莫名其妙掉血） |
| M3 | 地层规则（每 160m 换一套物理规则，与钻头等级一一对应） |
| M4 | 残骸回捞（死亡存货舱变成可挖回的地标） |
| M5 | 叙事层（术语替换 + 旧世界废料 + 终局发现区） |
| — | 存档（2.4h+ 的游戏没存档是劝退项）、商店 UI、手柄 |
